{
  "name": "quota-monitor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "check:port": "node scripts/check-port.ts",
    "check:port:ui": "node scripts/check-port-ui.ts",
    "typecheck": "node node_modules/typescript/bin/tsc -p web/tsconfig.json --pretty false && node node_modules/typescript/bin/tsc -p web/tsconfig.ui.json --pretty false --noEmit && node node_modules/typescript/bin/tsc -p tsconfig.tools.json --pretty false",
    "build:web": "node scripts/build-web.ts",
    "test": "node --test tests/*.test.ts",
    "check:ui": "node scripts/ui-check.ts",
    "check:ui:flow": "node scripts/ui-flow.ts"
  },
  "devDependencies": {
    "@types/node": "^24.13.6",
    "typescript": "5.6.3"
  }
}
